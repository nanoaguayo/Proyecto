<h1>Catalogo</h1>
<section class="catalogo">
<% @productos.each do |p| %>
	
		<article class="photo">
			<div class="image-cont">
				<a href=""><%=image_tag asset_path(p.image)%></a>
			</div>
			<div class="text-cont">
		        <span class="description"><%= p.title %></span>
		        <span class="price">$<%=p.price%></span>
		        <span class="description">Categoria: <%= p.category %></span>
		    </div>
		</article>
<%end%>
</section>