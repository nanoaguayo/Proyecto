<h1>Catalogo</h1>
<section class="catalogo">
<% @productos.each do |p| %>
	
		<article class="photo">
			<div class="image-cont">
				<%=link_to (image_tag asset_path(p.image)), show_path(:product_id => p.id)%>
			</div>
			<div class="text-cont">
		        <span class="description"><%= p.title %></span>
		        <span class="price">$<%=p.price%></span>
		        <span class="description">Categoria: <%= p.category %></span>
		    </div>
		</article>
<%end%>
</section>